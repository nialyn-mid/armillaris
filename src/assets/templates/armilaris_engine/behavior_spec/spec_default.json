{
  "description": "Defines how Graph Data is injected into the Engine",
  "root": {
    "nodes": {
      "$for": "nodes",
      "$item": {
        "id": "{{id}}",
        "label": "{{label}}",
        "data": "{{properties}}"
      }
    },
    "adj": "{{adjacency}}"
  }
}